// @version=5
// Auto-generated by omni-ict pine_codegen.py. Do not edit by hand.
indicator("OMNI Signal Overlay", overlay=true, max_lines_count=500, max_labels_count=500)

// ── Config ──────────────────────────────────────────────
bullCol = input.color(color.new(color.teal,    0),  "Bull color")
bearCol = input.color(color.new(color.red,     0),  "Bear color")
slCol   = input.color(color.new(color.orange,  0),  "SL color")
tpCol   = input.color(color.new(color.green,   0),  "TP color")
lineW   = input.int(2, "Line width", minval=1, maxval=5)

// ── Signals ───────────────────────────────
// [0] XAUUSD-M15-1781747217444122-BEAR
if barstate.islast
    line.new(bar_index - 50, 4122.97, bar_index, 4122.97, color=bearCol, width=lineW)
    line.new(bar_index - 50, 4135.31732, bar_index, 4135.31732, color=slCol, style=line.style_dashed, width=1)
    line.new(bar_index - 50, 4038.69, bar_index, 4038.69, color=tpCol, style=line.style_dashed, width=1)
    label.new(bar_index, 4122.97, "XAUUSD BEAR fvg_entry (1.00)", color=bearCol, textcolor=color.white, style=label.style_label_left, size=size.small)
